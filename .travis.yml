language: java
branches:
  except:
    - /.*-Stable$/
install: nvm install stable
script:
    - cd bin
    - npm ci
    - cd ..
    - ./build zel zcommon zweb zk zul zkbind zhtml zkplus-legacy
# keep the npm cache around to speed up installs
cache:
  directories:
  - "$HOME/.npm"