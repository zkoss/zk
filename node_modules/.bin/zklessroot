#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../zkless-engine/bin/zklessroot" "$@"
  ret=$?
else 
  node  "$basedir/../zkless-engine/bin/zklessroot" "$@"
  ret=$?
fi
exit $ret
