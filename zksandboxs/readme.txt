Used to build zkdemo.ear.

* It is useful for J2EE servers that support only EAR. Example: Oracle OC4J.
* We deploy it as lib because we don't test it directly. Rather, used only
  as part of the distribution.
